enum Endpoints {
  Home = '/',
  Login = '/login',
  SignUp = '/signup',
  Users = '/users',
  Statuses = '/statuses',
  Labels = '/labels',
  Tasks = '/tasks',
  Wildcard = '*',
}

export default Endpoints;
