enum Endpoints {
  Home = '/',
  Login = '/login',
  SignUp = '/signup',
  Users = '/users',
  UserEdit = '/users/:id/edit',
  Statuses = '/statuses',
  Labels = '/labels',
  Tasks = '/tasks',
  Wildcard = '*',
}

export default Endpoints;
